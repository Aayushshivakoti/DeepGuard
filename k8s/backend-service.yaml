apiVersion: v1
kind: Service
metadata:
  name: deepguard-backend-service
  namespace: default
spec:
  selector:
    app: deepguard-backend
  ports:
    - protocol: TCP
      port: 8000
      targetPort: 8000
  type: ClusterIP
